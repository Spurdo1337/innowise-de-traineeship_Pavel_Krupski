# Pagila SQL Test Tasks

Pagila (PostgreSQL demo DB) + solutions to 7 analytical SQL tasks.
Runs fully automatically on container start — no manual steps inside
the container needed.

## Run

```bash
docker compose up --build -d
```

Results for all 7 tasks will be in `output/results.txt`.

To stop the container:

```bash
docker compose down
```
